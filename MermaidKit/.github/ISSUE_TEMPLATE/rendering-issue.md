---
name: Diagram renders wrong (or not at all)
about: A Mermaid source that MermaidKit mis-renders, drops, or rejects
labels: rendering
---

**The Mermaid source** (paste the exact text):

```mermaid

```

**Expected** — what mermaid.live shows (screenshot welcome):

**Actual** — what MermaidKit did (`nil` from parse? rendered but wrong?
screenshot welcome):

**Platform**: macOS/iOS version, MermaidKit version/commit.
