# Blocks & Callouts

> [!NOTE]
> Five semantic callout types with distinct tints.

> [!TIP]
> Use fixtures to preview every element.

> [!WARNING]
> Warnings are orange.

> A blockquote with `inline code`, **bold**, and a nested code card:
>
> ```swift
> let rendered = renderer.render(document)
> ```

| Feature | Status | Notes |
| :--- | :---: | ---: |
| Tables | ✓ | 3 alignments |
| Task lists | ✓ | 12 |

- [x] Checkboxes toggle with a click
- [ ] Round-trip stays byte-lossless
